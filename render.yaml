services:
  - type: web
    name: kcp-estimate
    env: python
    buildCommand: |
      apt-get update
      apt-get install -y wkhtmltopdf
      pip install -r requirements.txt
      python manage.py collectstatic --no-input
      python manage.py migrate
    startCommand: gunicorn KCP.wsgi:application
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0 